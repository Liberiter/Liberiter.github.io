---
title: "Astro Content Collections의 generateId"
tags: [astro]
---

- Astro 5의 `glob()` 로더는 기본적으로 파일 경로 전체를 id로 쓴다 (`engineers-guild/foo.md`).
- `generateId` 옵션으로 파일명만 남기면 URL에서 폴더를 뗄 수 있다 → 글의 장소를 옮겨도 링크가 안 깨진다.
- 대신 파일명(슬러그)이 전역에서 유일해야 한다는 규칙이 생김. 기억할 것.
